# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/

# Node
node_modules/
*.js.map

# Data dirs created at runtime
/dispatcher/.venv/

# Local dev MCP config at repo root — not committed.
# Scoped to root so it never shadows the plugin's own plugin/voice/.mcp.json.
/.mcp.json
